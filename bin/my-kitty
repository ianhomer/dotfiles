#!/usr/bin/env python3

# import json
import os

print("Initialising my kitty")

# state = json.loads()

os.system('kitty @ launch --type=tab --tab-title dots --cwd ~/.dotfiles nvim

# kitty @ launch --type=tab --tab-title things --cwd ~/projects/things nvim
