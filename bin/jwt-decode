#!/bin/bash

for i in 1 2 ; do
  echo
  echo "Part $i"
  part=$(echo $1 | cut -f$i -d .)
  case $((${#part} % 4)) in
    2) part="${part}==";;
    3) part="${part}=";;
  esac
  echo $part
  echo $part | base64 -d
done
echo

