#!/usr/bin/env bash

$(shim)
crypt="$HOME/local/box"
recipient=${1:-$MY_RECIPIENT}

if [ -z "$recipient" ] ; then
   echo "recipient is empty"
   exit 1
fi

if [ ! -d "$crypt" ] ; then
  echo "Crypt directory $crypt does not exist"
  exit 1
fi

for file in $crypt/*.gpg ; do
  echo $file
done


