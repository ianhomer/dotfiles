#!/usr/bin/env bash

#
# Test log functions
#

_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
. ${_DIR}/functions/log.sh

_ message
o_o message
_ info with multiple words
_ status OK status
_ error with $(bold message)
