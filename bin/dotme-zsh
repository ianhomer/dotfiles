#!/usr/bin/env zsh
#
# Configure zsh
#

should-run -m 7 dotme-zsh || exit 0

$(shim) && cd ${ME} && . bin/i.sh

log:: "Configuring zsh shell"

ZSH=~/.oh-my-zsh

if [[ -d ~/.oh-my-zsh ]] ; then
  log::warn "~/.oh-my-zsh directory exists, please remove this directory"
fi

should-run -fu dotme-zsh
